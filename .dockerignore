# 版本控制
.git
.gitignore

# 依赖（构建时重新安装）
node_modules
mini-services/*/node_modules

# 构建缓存
.next

# 运行时数据（通过 Volume 持久化）
db/*.db
db/*.db-journal
config/auth.json
config/sessions.json
config/database.json

# 开发文件
dev.log
*.log
upload
worklog.md
agent-ctx
.zscripts
.z-ai-config
.claude
.claude*

# Docker 文件自身（不需要在镜像内）
Dockerfile
docker-compose.yml

# 其他
examples
scripts
skills
src/app_backup
download
*.tar.gz
.env*
!.env.example
